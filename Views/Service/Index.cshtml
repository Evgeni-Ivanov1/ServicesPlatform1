@model IEnumerable<ReservationPlatform.Models.Service>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Services</h1>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Category</th>
            <th>Price</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var service in Model)
        {
            <tr>
                <td>@service.Name</td>
                <td>@service.Description</td>
                <td>@service.Category</td>
                <td>@service.Price.ToString("C")</td>
                <td>
                    <a class="btn btn-info" asp-controller="Service" asp-action="Details" asp-route-id="@service.Id">View Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
