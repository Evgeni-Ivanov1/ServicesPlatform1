@using ServicesPlatform.Data.Models
@model IEnumerable<Review>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Service Reviews</h1>

@if (!Model.Any())
{
    <p>No reviews yet.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Username</th>
                <th>Comment</th>
                <th>Rating</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var review in Model)
            {
                <tr>
                    <td>@review.Username</td>
                    <td>@review.Comment</td>
                    <td>@review.Rating</td>
                </tr>
            }
        </tbody>
    </table>
}

<a class="btn btn-primary" asp-controller="Review" asp-action="Create" asp-route-serviceId="@ViewBag.ServiceId">Add Review</a>
